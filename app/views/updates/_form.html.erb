<%= form_for(@update, multipart: true) do |f| %>
  
  <% if @update.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@update.errors.count, "error") %> prohibited this update from being saved:</h2>

      <ul>
      <% @update.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class='field-group'>
    File Type: <%= f.select :data_type, [['Trails', 'Trail'], ['Pois', 'Pointsofinterest'], 'trails_descs'] %>
  </div>
  <div class='field-group'>
    Upload File: <%= f.file_field :data_file, accept: 'text/csv' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
