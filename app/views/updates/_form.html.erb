<%= form_for(@update, multipart: true) do |f| %>
  
  <% if @update.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@update.errors.count, "error") %> prohibited this update from being saved:</h2>

      <ul>
      <% @update.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class='form-group'>
    <label for="data_type" class="col-sm-2 control-label">File Type: </label>
    <%= f.select :data_type, [ ['Pois', 'Pointsofinterest']], {}, { class: "form-control col-sm-14" } %>
  </div>
  <div class='form-group'>
    Upload File: <%= f.file_field :file, accept: 'text/csv', required: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
