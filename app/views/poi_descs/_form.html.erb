<%= form_for(@poi_desc) do |f| %>
  <% if @poi_desc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poi_desc.errors.count, "error") %> prohibited this poi_desc from being saved:</h2>

      <ul>
      <% @poi_desc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<% @poi_desc.attributes.each do |attribute| %>
    <div class="field">
      <%= f.label attribute[0] %><br>
      <%= f.text_field attribute[0] %>
  </div>
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
